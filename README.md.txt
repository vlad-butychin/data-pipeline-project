# Data Pipeline Project

Учебный проект, демонстрирующий полный цикл работы с данными: загрузка, валидация, очистка, обучение модели, отчётность и визуализация.

## 📌 Возможности
- Загрузка данных из:
  - CSV, Excel
  - SQL (через SQLAlchemy)
  - API (через `requests`)
- Валидация:
  - Пропуски
  - Типы данных
  - Дубликаты
  - Выбросы (Z-score)
- Очистка:
  - Заполнение пропусков медианой/модой
  - Удаление колонок с большим количеством пропусков
  - Преобразование дат
- Модели:
  - Линейная регрессия
  - Логистическая регрессия
  - Автоматический выбор задачи (classification/regression)
  - Метрики качества (R2, RMSE, Accuracy, Precision, Recall, F1)
  - Feature importance
- Отчёты:
  - Excel (`openpyxl`)
  - PDF (`reportlab`)
- Визуализация:
  - Гистограммы
  - Корреляционная матрица (heatmap)
- Логирование (`logging`)

## ⚙️ Установка
```bash
pip install pandas numpy sqlalchemy scipy scikit-learn matplotlib seaborn openpyxl reportlab requests

== ЗАПУСК ПРОЕКТА
from pipeline import main_pipeline

model, report = main_pipeline("data.csv", target="price")


data.csv — путь к вашему датасету

target — имя целевой переменной

После запуска:

Логи сохраняются в pipeline.log и validation.log

Отчёты: report.xlsx, report.pdf

Визуализации выводятся в окно

📂 Структура
from_csv, from_excel, from_api, from_sql — загрузка данных

validation — проверка качества данных

data_clean1 — очистка

train_model — обучение модели

generate_report — генерация отчёта

save_report_excel, save_report_pdf — сохранение отчётов

plot_hist, plot_correlation — визуализация

main_pipeline — объединяет все шаги
